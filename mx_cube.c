#include <stdio.h>

void mx_cube(int n)
{
    for (int i = 0; i < (n + n / 2 + 3); i++)
    {
        for (int j = 0; j < n / 2 + 2 + 2 + n * 2)
        {
        }
    }
}

void main()
{
    mx_cube(2);
}